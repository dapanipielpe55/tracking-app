workflows:
  android-build:
    name: Android Build
    environment:
      node: latest
    scripts:
      - cd android
      - gradle wrapper --gradle-version 8.1.0
      - chmod +x gradlew
      - ./gradlew assembleDebug --stacktrace
    artifacts:
      - android/app/build/outputs/**/*.apk
