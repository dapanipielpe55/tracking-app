workflows:
  android-build:
    name: Android Build
    environment:
      node: latest
    scripts:
      - cd android
      - chmod +x gradlew || true
      - ./gradlew assembleDebug --stacktrace --debug
    artifacts:
      - android/app/build/outputs/**/*.apk
