workflows:
  android-build:
    name: Android Build
    environment:
      java: 17

    scripts:
      - cd android
      - chmod +x gradlew
      - ./gradlew assembleDebug --stacktrace

    artifacts:
      - android/app/build/outputs/**/*.apk
